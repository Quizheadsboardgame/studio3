
{
  "entities": {
    "Seller": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Seller",
      "type": "object",
      "description": "Represents an individual seller of collectible cards.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Seller entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the seller."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Sale": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Sale",
      "type": "object",
      "description": "Records an individual transaction for a collectible card.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Sale entity."
        },
        "cardName": {
          "type": "string",
          "description": "The name of the collectible card sold."
        },
        "price": {
          "type": "number",
          "description": "The sale price of the card, in the local currency (e.g., GBP)."
        },
        "saleDate": {
          "type": "string",
          "description": "The date when the sale was recorded, in YYYY-MM-DD format.",
          "format": "date"
        },
        "sellerId": {
          "type": "string",
          "description": "Reference to the Seller who made this sale."
        }
      },
      "required": [
        "id",
        "cardName",
        "price",
        "saleDate",
        "sellerId"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/profiles/{profileId}/sellers/{sellerId}",
        "definition": {
          "entityName": "Seller",
          "schema": {
            "$ref": "#/backend/entities/Seller"
          },
          "description": "Shared seller profiles for a specific role (Manager or Staff).",
          "params": [
            {
              "name": "profileId",
              "description": "The ID of the profile (e.g., 'manager', 'staff')."
            },
            {
              "name": "sellerId",
              "description": "The unique identifier for a specific Seller entity."
            }
          ]
        }
      },
      {
        "path": "/profiles/{profileId}/sales/{saleId}",
        "definition": {
          "entityName": "Sale",
          "schema": {
            "$ref": "#/backend/entities/Sale"
          },
          "description": "Shared sales transactions for a specific role (Manager or Staff).",
          "params": [
            {
              "name": "profileId",
              "description": "The ID of the profile (e.g., 'manager', 'staff')."
            },
            {
              "name": "saleId",
              "description": "The unique identifier for a specific Sale transaction."
            }
          ]
        }
      }
    ]
  }
}
